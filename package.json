{
  "name": "weather",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "rm -rf node_modules/.cache && rimraf out && next build && next export && touch out/.nojekyll && touch out/CNAME && echo \"vremea.ionkom.com\" >> out/CNAME && gh-pages -d out -t true"
  },
  "dependencies": {
    "@apollo/client": "^3.2.0",
    "@fast-csv/parse": "^4.3.2",
    "bootstrap": "^4.5.2",
    "graphql": "^15.3.0",
    "next": "9.5.3",
    "react": "16.13.1",
    "react-bootstrap": "^1.3.0",
    "react-dom": "16.13.1",
    "swr": "^0.3.5"
  },
  "devDependencies": {
    "@types/node": "^14.6.3",
    "@types/react": "^16.9.49",
    "gh-pages": "^3.1.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2"
  }
}
